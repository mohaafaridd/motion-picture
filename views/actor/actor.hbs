<div class="container p-5">
    <div class="card shadow rounded">
        {{!-- Card Body --}}
        <div class="card-body p-0">
            {{!-- Actor info --}}
            <div class="media">
                <img src="http://image.tmdb.org/t/p/w500{{actor.pic}}" class="mr-3" alt="{{actor.name}} picture">
                <div class="media-body">
                    <h1>{{actor.name}}</h1>
                    <p>{{actor.bio}}</p>
                </div>
            </div>
            {{!-- Actor info --}}
        </div>
        {{!-- Card Body --}}
    </div>
</div>