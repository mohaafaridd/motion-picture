<h1>{{user.name}}'s list</h1>
<h2>{{list.name}}</h2>

{{#each content}}
<h2>{{title}}</h2>
<p>{{overview}}</p>
<img src="http://image.tmdb.org/t/p/w500{{poster}}" alt="{{title}} poster">

<form action="/media/{{type}}/{{id}}" method="get">
    <button type="submit">View {{type}}</button>
</form>

{{/each}}